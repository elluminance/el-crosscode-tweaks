ig.EFFECT_ENTRY.SET_SHADOW = ig.EffectStepBase.extend({
    
    //the action step version does everything this needs to do anyways.
    init: ig.ACTION_STEP.SET_SHADOW.prototype.init,
    start: ig.ACTION_STEP.SET_SHADOW.prototype.start
})